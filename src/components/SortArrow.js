import React from 'react'

const SortArrow = (props) => (

        <img src={props.sortImg}
          className={"arrow-up"}
          alt="sort-arrow-up"
          title="Sort-up" 
          />
  
)

export default SortArrow